#!/usr/bin/env sh
set -e

HUSKY_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
ROOT_DIR="$(CDPATH= cd -- "${HUSKY_DIR}/.." && pwd)"

MARKER_FILE="${ROOT_DIR}/.git/.vault-welcome-shown"

if [ -f "${MARKER_FILE}" ]; then
    exit 0
fi

cat <<EOF

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Welcome to Vault Core (Hardhat)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Useful commands:

  make check      Run all quality checks
  npm test        Run tests
  npm run lint    Lint Solidity code

Assessment instructions:

  See ISSUE.md

Husky hooks are pre-configured.
You do not need to modify them.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EOF

touch "${MARKER_FILE}"

exit 0
