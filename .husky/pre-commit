#!/usr/bin/env sh
set -e

echo "Running pre-commit checks..."

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

sh "${SCRIPT_DIR}/scripts/check.sh"

npm run lint || exit 1
npm test || exit 1

echo "✓ Pre-commit checks passed."
